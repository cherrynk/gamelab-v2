# DeepSeek (OpenAI-compatible). Copy to .env.local — never commit the key.
DEEPSEEK_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-v4-flash

# Optional. If set, blocked new playstyles are implemented by a local Cursor agent.
CURSOR_API_KEY=
